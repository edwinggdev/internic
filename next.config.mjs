/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    eslint: {
        ignoreDuringBuilds: true,
      },
      // basePath: '/internic', 
      // assetPrefix: '/internic',
};

export default nextConfig;
